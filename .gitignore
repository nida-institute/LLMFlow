# Outputs (generated artifacts)
outputs/
*.out
*.log
llmflow.log

# Swap and temp files (Vim, etc.)
*.swp
*.swo
*.swn
*~
*.bak
*.tmp

# Editor-specific files
.DS_Store
.idea/

# Claude Code — local session state and AI memory (never commit)
.claude/*
# ...except the reviewed permission set. Committed on purpose (2026-08-21): an allowlist
# that nobody can diff is how `git push:*`, `gh pr *` and `gh issue:*` came to be granted
# here while CLAUDE.md called all three hard stops. settings.local.json stays ignored.
!.claude/settings.json

# VSCode (allow settings.json for Copilot integration)
.vscode/*
!.vscode/settings.json

# Virtual environments
.venv/
env/
venv/

# Python
__pycache__/
*.pyc
*.pyo
*.py[cod]
*$py.class
*.so
.Python

# Hatch's build artifacts
dist/
build/
.eggs/
*.egg-info/

# Testing
.pytest_cache/
.coverage
htmlcov/
*.cover
.hypothesis/

# Environment
.env
.env.local
*.env

# LLMFlow specific
pseaf.txt
scenes/
nuitka_dist/
nuitka_build.log

# Input file backups
inputs/**/*.bak
inputs/**/*.tmp
inputs/**/*.xml~

obsidian/
FOO
chat.json
inputs/Abbot-Smith/macula-hebrew.tsv
CLAUDE.md
