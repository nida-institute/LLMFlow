"""sp developer/collaboration tools (`sp tools <tool>`).

Tools that consume artifacts sp already produces (debug request/response files,
intermediate outputs) to support the edit→observe loop, with no new
instrumentation. First member: `replay`.
"""
